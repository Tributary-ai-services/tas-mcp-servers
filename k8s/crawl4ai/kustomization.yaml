apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: crawl4ai

namespace: tas-mcp-servers

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - ingress.yaml

commonLabels:
  project: tas
  tier: shared-infrastructure
  service: crawl4ai
