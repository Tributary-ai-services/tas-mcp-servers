apiVersion: v1
kind: Service
metadata:
  name: crawl4ai
  namespace: tas-mcp-servers
  labels:
    app: crawl4ai
    component: web-scraper
spec:
  type: ClusterIP
  ports:
  - port: 11235
    targetPort: 11235
    protocol: TCP
    name: http
  selector:
    app: crawl4ai
