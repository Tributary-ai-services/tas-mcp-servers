apiVersion: v1
kind: Service
metadata:
  name: minio-mcp
  namespace: tas-mcp-servers
  labels:
    app: minio-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: minio-mcp
