apiVersion: v1
kind: ConfigMap
metadata:
  name: minio-mcp-config
  namespace: tas-mcp-servers
  labels:
    app: minio-mcp
    component: mcp-server
data:
  MINIO_ENDPOINT: "minio-shared.tas-shared.svc.cluster.local"
  MINIO_PORT: "9000"
  MINIO_USE_SSL: "false"
