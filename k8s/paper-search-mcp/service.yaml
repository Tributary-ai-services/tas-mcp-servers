apiVersion: v1
kind: Service
metadata:
  name: paper-search-mcp
  namespace: tas-mcp-servers
  labels:
    app: paper-search-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: paper-search-mcp
    project: tas
    service: paper-search-mcp
    tier: mcp-servers
