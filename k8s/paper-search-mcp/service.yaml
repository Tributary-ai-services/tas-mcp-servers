apiVersion: v1
kind: Service
metadata:
  name: paper-search-mcp
  namespace: tas-mcp-servers
  labels:
    app: paper-search-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: paper-search-mcp
