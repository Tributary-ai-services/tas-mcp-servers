apiVersion: v1
kind: Service
metadata:
  name: context7-mcp
  namespace: tas-mcp-servers
  labels:
    app: context7-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: context7-mcp
