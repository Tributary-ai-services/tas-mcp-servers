apiVersion: v1
kind: Service
metadata:
  name: atlassian-mcp
  namespace: tas-mcp-servers
spec:
  type: ClusterIP
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: http
  selector:
    app: atlassian-mcp
