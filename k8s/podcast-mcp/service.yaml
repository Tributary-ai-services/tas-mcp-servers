apiVersion: v1
kind: Service
metadata:
  name: podcast-mcp
  namespace: tas-mcp-servers
  labels:
    app: podcast-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8092
    targetPort: 8092
    protocol: TCP
    name: http
  selector:
    app: podcast-mcp
