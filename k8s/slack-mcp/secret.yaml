apiVersion: v1
kind: Secret
metadata:
  name: slack-mcp-secret
  namespace: tas-mcp-servers
  labels:
    app: slack-mcp
    component: mcp-server
type: Opaque
stringData:
  # Replace with actual Slack Bot Token (xoxb-...)
  SLACK_BOT_TOKEN: "REPLACE_ME"
  # Replace with actual Slack Team/Workspace ID (T...)
  SLACK_TEAM_ID: "REPLACE_ME"
