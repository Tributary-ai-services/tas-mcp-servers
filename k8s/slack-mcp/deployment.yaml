apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-mcp
  namespace: tas-mcp-servers
  labels:
    app: slack-mcp
    component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slack-mcp
  template:
    metadata:
      labels:
        app: slack-mcp
        component: mcp-server
    spec:
      containers:
      - name: slack-mcp
        image: registry-api.tas.scharber.com/tas-mcp-bridge:1.0.0
        imagePullPolicy: Always
        env:
        - name: MCP_COMMAND
          value: "npx"
        - name: MCP_ARGS
          value: "-y @modelcontextprotocol/server-slack"
        - name: BRIDGE_PORT
          value: "8000"
        - name: BRIDGE_NAME
          value: "slack-mcp"
        - name: BRIDGE_VERSION
          value: "1.0.0"
        envFrom:
        - secretRef:
            name: slack-mcp-secret
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 45
          periodSeconds: 10
          failureThreshold: 3
