apiVersion: v1
kind: Service
metadata:
  name: slack-mcp
  namespace: tas-mcp-servers
  labels:
    app: slack-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: slack-mcp
