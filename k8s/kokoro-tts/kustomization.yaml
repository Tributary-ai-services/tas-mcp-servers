apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: kokoro-tts

namespace: tas-mcp-servers

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  project: tas
  tier: mcp-servers
  service: kokoro-tts
