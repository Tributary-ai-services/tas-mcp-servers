apiVersion: v1
kind: Service
metadata:
  name: kokoro-tts
  namespace: tas-mcp-servers
spec:
  type: ClusterIP
  ports:
  - port: 8880
    targetPort: 8880
    protocol: TCP
    name: http
  selector:
    app: kokoro-tts
