apiVersion: v1
kind: Service
metadata:
  name: neo4j-mcp
  namespace: tas-mcp-servers
  labels:
    app: neo4j-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: neo4j-mcp
