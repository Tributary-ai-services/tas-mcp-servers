apiVersion: v1
kind: Service
metadata:
  name: neo4j-mcp
  namespace: tas-mcp-servers
  labels:
    app: neo4j-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: neo4j-mcp
    project: tas
    service: neo4j-mcp
    tier: mcp-servers
