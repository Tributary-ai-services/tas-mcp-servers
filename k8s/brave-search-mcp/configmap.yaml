apiVersion: v1
kind: ConfigMap
metadata:
  name: brave-search-mcp-config
  namespace: tas-mcp-servers
data:
  BRAVE_MCP_TRANSPORT: "http"
  BRAVE_MCP_PORT: "8000"
  BRAVE_MCP_HOST: "0.0.0.0"
