apiVersion: apps/v1
kind: Deployment
metadata:
  name: brave-search-mcp
  namespace: tas-mcp-servers
  labels:
    app: brave-search-mcp
    component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: brave-search-mcp
  template:
    metadata:
      labels:
        app: brave-search-mcp
        component: mcp-server
    spec:
      containers:
      - name: brave-search-mcp
        image: mcp/brave-search:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: brave-search-mcp-config
        - secretRef:
            name: brave-search-mcp-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
