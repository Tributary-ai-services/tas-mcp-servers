apiVersion: v1
kind: Service
metadata:
  name: brave-search-mcp
  namespace: tas-mcp-servers
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: brave-search-mcp
