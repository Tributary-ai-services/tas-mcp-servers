apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tas-mcp-servers
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
commonLabels:
  project: tas
  tier: mcp-servers
  service: sequential-thinking-mcp
