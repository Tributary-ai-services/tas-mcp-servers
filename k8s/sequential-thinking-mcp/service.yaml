apiVersion: v1
kind: Service
metadata:
  name: sequential-thinking-mcp
  namespace: tas-mcp-servers
  labels:
    app: sequential-thinking-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: sequential-thinking-mcp
