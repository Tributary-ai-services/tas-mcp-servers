apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-mcp-config
  namespace: tas-mcp-servers
  labels:
    app: postgres-mcp
    component: mcp-server
    service: postgres-mcp
data:
  DATABASE_URL: "postgresql://tasuser:taspassword@postgres-shared.tas-shared.svc.cluster.local:5432/tas_shared?sslmode=disable"
