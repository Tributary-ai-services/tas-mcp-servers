apiVersion: v1
kind: Service
metadata:
  name: postgres-mcp
  namespace: tas-mcp-servers
  labels:
    app: postgres-mcp
    component: mcp-server
    service: postgres-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: postgres-mcp
    service: postgres-mcp
