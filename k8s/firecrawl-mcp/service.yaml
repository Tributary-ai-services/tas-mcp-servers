apiVersion: v1
kind: Service
metadata:
  name: firecrawl-mcp
  namespace: tas-mcp-servers
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: firecrawl-mcp
