apiVersion: v1
kind: Service
metadata:
  name: dbhub
  namespace: tas-mcp-servers
  labels:
    app: dbhub
    component: database-mcp
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: dbhub
