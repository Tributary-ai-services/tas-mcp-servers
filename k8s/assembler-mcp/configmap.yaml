apiVersion: v1
kind: ConfigMap
metadata:
  name: assembler-mcp-config
  namespace: tas-mcp-servers
  labels:
    app: assembler-mcp
data:
  MINIO_ENDPOINT: "minio-shared.tas-shared.svc.cluster.local:9000"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_BUCKET: "assembler-outputs"
  MINIO_USE_SSL: "false"
