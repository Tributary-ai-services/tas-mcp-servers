apiVersion: v1
kind: Service
metadata:
  name: assembler-mcp
  namespace: tas-mcp-servers
  labels:
    app: assembler-mcp
spec:
  selector:
    app: assembler-mcp
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
      name: http
  type: ClusterIP
