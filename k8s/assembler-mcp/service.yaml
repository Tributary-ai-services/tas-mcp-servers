apiVersion: v1
kind: Service
metadata:
  name: assembler-mcp
  namespace: tas-mcp-servers
  labels:
    app: assembler-mcp
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8091
    protocol: TCP
    targetPort: 8091
  selector:
    app: assembler-mcp
