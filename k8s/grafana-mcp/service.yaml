apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp
  namespace: tas-mcp-servers
  labels:
    app: grafana-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: grafana-mcp
    project: tas
    service: grafana-mcp
    tier: mcp-servers
