apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-mcp-config
  namespace: tas-mcp-servers
  labels:
    app: grafana-mcp
    component: mcp-server
data:
  GRAFANA_URL: "http://grafana-shared.tas-shared.svc.cluster.local:3000"
