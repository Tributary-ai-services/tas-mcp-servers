apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: tas-mcp-servers

resources:
  - namespace.yaml
  - crawl4ai
  - napkin-mcp
  - napkin-operator
  - napkin-federation-init.yaml
  - neo4j-mcp
  - minio-mcp
  - kafka-mcp
  - grafana-mcp

commonLabels:
  project: tas
  tier: mcp-servers
