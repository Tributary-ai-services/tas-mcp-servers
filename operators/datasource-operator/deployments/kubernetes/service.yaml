apiVersion: v1
kind: Service
metadata:
  name: datasource-operator
  namespace: tas-mcp-servers
  labels:
    app: datasource-operator
spec:
  selector:
    app: datasource-operator
  ports:
    - name: metrics
      port: 8088
      targetPort: 8088
    - name: health
      port: 8089
      targetPort: 8089
