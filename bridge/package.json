{
  "name": "@tas-mcp/bridge",
  "version": "2.0.0",
  "type": "module",
  "description": "Connection-aware MCP bridge that wraps stdio MCP servers with HTTP endpoints and dynamic connection injection",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
