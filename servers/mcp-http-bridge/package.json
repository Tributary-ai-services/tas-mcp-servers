{
  "name": "@tas-mcp/http-bridge",
  "version": "1.0.0",
  "description": "Generic stdio-to-HTTP bridge for MCP servers. Wraps any stdio MCP server with TAS-compatible HTTP endpoints (/health, /mcp/tools/list, /mcp/tools/call).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
